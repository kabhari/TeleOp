syntax = "proto3";

service Coordinate{
    rpc SendCoordination(stream CoordinateRequest) returns (CoordinateResponse);
}

message CoordinateRequest {
    double x = 1;
    double y = 2;
}

message CoordinateResponse {
    string message  = 1;
}